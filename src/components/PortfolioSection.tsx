// Portfolio intentionally blank for now

export function PortfolioSection() {
  return (
    <section id="portfolio" className="section" aria-labelledby="portfolio-heading">
      <div className="container flex flex-col gap-4">
        <h2 id="portfolio-heading" className="sr-only">Portfolio</h2>
        {/* Placeholder: portfolio items coming soon */}
      </div>
    </section>
  );
}
